# 【开发】环境变量

# 应用部署基础路径（如部署在子目录 /admin，则设置为 /admin/）
VITE_BASE_URL = /

# API 请求基础路径（开发/生产均使用同域 /api）
VITE_API_URL = /api

# 代理目标地址：应指向本地 Webman（hgt.test），转发后路径为 /core/*
# 不要指向生产 manage 域名；也不要依赖跨域直连生产 API
VITE_API_PROXY_URL = http://hgt.test

# Delete console
VITE_DROP_CONSOLE = false
